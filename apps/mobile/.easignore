# Keep EAS Android upload lean; native source is included, generated build outputs are not.
# Android is gitignored in the Expo app, so explicit negation is required for EAS archives.
!android/
!android/**
android/app/build/
android/app/.cxx/
android/build/
android/.gradle/
.expo/

# Local screenshots/static exports from design iterations.
dist-android*/

# Local env files are supplied through eas.json/profile env.
.env*

# Package-manager and OS noise.
node_modules/
.DS_Store
*.log
