# CC Executive Summary

Date: 2026-04-09
Scope: current app surface and routing baseline only

## Current routes and routing state

- `/`
  - Implemented by `src/app/page.tsx`
  - Acts as the only concrete App Router page in the repo today
  - Includes signed-out marketing copy plus signed-in challenge/progress UI driven from Clerk metadata
- `/account`
  - Linked from the home page
  - Protected by `src/middleware.ts`
  - No matching `src/app/account/*` route exists in the repo yet, so the route is assumed but not implemented
- `/challenges`
  - Linked from the home page
  - No matching `src/app/challenges/*` route exists in the repo yet
- `/challenges/[id]`
  - Linked from the home page for active and suggested challenges
  - No matching dynamic route exists in the repo yet

## Current components and modules shaping the surface

- `src/app/layout.tsx`
  - Global shell, fonts, and `ClerkProvider`
- `src/app/page.tsx`
  - Monolithic landing + signed-in dashboard page
  - Handles auth-aware rendering, challenge summary cards, progress summary, CTA buttons, and local metadata parsing helpers in one file
- `src/lib/challenges.ts`
  - Static challenge catalog with three starter challenges and a lookup helper
- `src/middleware.ts`
  - Clerk middleware protecting `/account(.*)` only

## Product baseline

- The visible product promise is a Lichess-linked chess challenge flow: sign in, save a Lichess username, choose a challenge, play a game, then return for verification.
- The current implementation mostly presents that promise on the homepage but does not yet provide the linked route structure needed to complete the flow.
- Signed-in state depends on Clerk user public metadata for challenge state, progress, and latest attempt summaries.

## Most important product gap

The most important visible gap is that the homepage sells a multi-step product loop, but the core destination routes it points to (`/account`, `/challenges`, `/challenges/[id]`) are not implemented in the current app surface. That means the app has a promising front door but no reviewable in-product path to actually configure identity or start/inspect a challenge.

## Auth and deployment audit

### Auth assumptions in the current repo

- Clerk is the active auth provider, confirmed by `@clerk/nextjs` in `package.json`, `ClerkProvider` usage in `src/app/layout.tsx`, and Clerk middleware in `src/middleware.ts`.
- The app currently assumes client and server Clerk keys are supplied through environment variables.
- Route protection is narrow right now: only `/account(.*)` is explicitly protected in middleware.
- The homepage product loop appears to rely on Clerk user public metadata to store the Lichess username, challenge progress, and latest attempt state.

### Deployment assumptions in the current repo

- The deployment path appears to be Vercel, not Cloudflare or a custom container flow.
- Evidence for that assumption is limited but concrete: the repo still has the default Vercel-oriented Next.js README text and a local `.env.local` generated by the Vercel CLI.
- There is no `wrangler.toml`, Dockerfile, or alternative deployment config checked into the repo.
- `next.config.ts` is still essentially default, so there is no repo-level deployment tuning or adapter configuration yet.

### Open risks and unknowns

- The repo does not include a sanitized `.env.example`, so required auth variables and their expected environments are undocumented.
- Because only `/account` is protected, future signed-in challenge routes may accidentally ship without auth enforcement unless middleware expands with the product surface.
- The repo does not document which Vercel project, environments, or domains back this app, so the actual production path is still an operational unknown.
- If Clerk metadata is the source of truth for challenge state, there is not yet any visible schema contract or validation layer documenting that assumption.

## Verification note

Artifact updated at `cc/docs/EXECUTIVE_SUMMARY_2026-04-09.md` for the auth and deployment audit item. Local verification: `test -f docs/EXECUTIVE_SUMMARY_2026-04-09.md`.
