# Chess.com post-parity dual-host authenticated account-setting mutation round-trip repeatability streak extension plus six next step

Date: 2026-04-13
Owner: Sam

## Context

The current proof chain already includes nine fresh same-run dual-host authenticated `/account` Chess.com username-setting mutation round-trip parity runs, ending with `and72norcc13014759ny` and matching immediate post-submit retained-input state plus matching post-reload blank-input fallback state on both hosts.

## Tightest next step

The smallest next proof-bearing extension is one more fresh same-run signed-in dual-host `/account` round-trip repeatability recheck using one brand new narrow Chess.com username on both hosts.

## Why this is still the tightest extension

- It extends the existing authenticated `/account` mutation plus reload proof chain by exactly one run.
- It reuses the same narrow scope that has already produced stable parity evidence.
- It increases confidence in repeatability without widening into broader account-setting coverage or unrelated surfaces.
- Anything broader would add scope without being a tighter follow-up than a tenth fresh recheck.

## Required evidence for the next smoke

- Use the canonical host and active deployment host signed-in `/account` URLs.
- Submit one brand new narrow Chess.com username value on both hosts in the same run.
- Capture matching immediate post-submit state on both hosts.
- Reload `/account` on both hosts and capture matching post-reload state on both hosts.
- Record one shared proof window and end with a concise parity verdict.

## Explicit deferrals

- Broader account-settings coverage
- Backend fixes
- Deployment work
- Any new challenge-detail sweeps

## Verdict

After the completed nine-run authenticated account-setting mutation round-trip repeatability streak extension proof, one more fresh same-run signed-in dual-host `/account` recheck with a brand new narrow Chess.com username is still the exact tightest next step.
